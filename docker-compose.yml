version: "3.9"

services:
  api:
    build: .
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
